import ModalDialog, {ModalDialogProps} from './ModalDialog';
import ModalProvider, {
  ModalProviderProps,
  ModalRenderProps,
} from './ModalProvider';

export {ModalDialogProps};
export default ModalDialog;

export {ModalProvider, ModalProviderProps, ModalRenderProps};
